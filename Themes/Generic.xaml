<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:UIBlueprintEditor.Editor"
    xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore"
    xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fdev="FrostyDeveloper"
    xmlns:editors="clr-namespace:Frosty.Core.Controls.Editors;assembly=FrostyCore"
    xmlns:conv="clr-namespace:Frosty.Core.Converters;assembly=FrostyCore">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- UI Blueprint Editor -->
    <Style TargetType="{x:Type local:UIEditor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:UIEditor}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="PART_Column1" Width="1*"/>
                            <ColumnDefinition x:Name="PART_ColumnSplitter" Width="0"/>
                            <ColumnDefinition x:Name="PART_Column2" Width="0*" MinWidth="0"/>
                        </Grid.ColumnDefinitions>

                        <!-- Tabs -->
                        <Border Grid.Column="2" Grid.Row="1">
                            <Grid>
                                <ctrl:FrostyTabControl Name="PART_TabControl" Background="Transparent" Foreground="{StaticResource FontColor}">
                                    <ctrl:FrostyTabItem x:Name="PART_TabLayers" Icon="/UIBlueprintEditor;component/Images/Layers.png" Header="Layers" CloseButtonVisible="False">
                                        <ctrl:FrostyDockablePanel TitleBarVisible="False" Title="Layers" BorderVisible="False">
                                            <Rectangle Fill="Red"/>
                                        </ctrl:FrostyDockablePanel>
                                    </ctrl:FrostyTabItem>
                                    <ctrl:FrostyTabItem x:Name="PART_TabToolbox" Icon="/UIBlueprintEditor;component/Images/Toolbox.png" Header="Toolbox" CloseButtonVisible="False">
                                        <ctrl:FrostyDockablePanel TitleBarVisible="False" Title="Toolbox" BorderVisible="False">
                                            <Rectangle Fill="Lime"/>
                                        </ctrl:FrostyDockablePanel>
                                    </ctrl:FrostyTabItem>
                                    <ctrl:FrostyTabItem x:Name="PART_TabProperties" Icon="/FrostyEditor;component/Images/Properties.png" Header="Properties" CloseButtonVisible="False">
                                        <ctrl:FrostyDockablePanel TitleBarVisible="False" Title="Properties" BorderVisible="False">
                                            <core:FrostyPropertyGrid x:Name="PART_TabPropertiesContent" Asset="{Binding Asset, RelativeSource={RelativeSource TemplatedParent}}" Modified="{Binding AssetModified, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                                        </ctrl:FrostyDockablePanel>
                                    </ctrl:FrostyTabItem>
                                </ctrl:FrostyTabControl>
                            </Grid>
                        </Border>

                        <GridSplitter Focusable="False" Grid.Column="1" Background="Transparent" VerticalAlignment="Stretch" Width="3" ResizeDirection="Columns" HorizontalAlignment="Center" ResizeBehavior="PreviousAndNext"/>

                        <Grid>
                            <ctrl:FrostyDockablePanel x:Name="PART_UIEditorLayer" Visibility="Hidden" Grid.Column="0" Title="" TitleBarVisible="False">
                                <Grid ClipToBounds="True">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="24"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Grid.Background>
                                        <ImageBrush x:Name="PART_BackgroundGrid" ImageSource="/UIBlueprintEditor;component/Images/Grid.png" TileMode="Tile" Viewport="0,0,28,28" ViewportUnits="Absolute" Stretch="None"/>
                                    </Grid.Background>

                                    <Border Panel.ZIndex="10">
                                        <DockPanel>
                                            <Border Background="{StaticResource ControlBackground}" Margin="-2"/>
                                        </DockPanel>
                                    </Border>

                                    <!-- UI Editor -->
                                    <Button Panel.ZIndex="10" x:Name="PART_AddObject" Visibility="Collapsed" HorizontalAlignment="Left" Width="25" Height="25" Margin="2,0,0,0" ToolTip="Add a UI item" IsEnabled="True">
                                        <Button.Content>
                                            <Image Width="14" Source="/FrostyEditor;component/Images/Add.png"/>
                                        </Button.Content>
                                    </Button>

                                    <Button Panel.ZIndex="10" x:Name="PART_Refresh" HorizontalAlignment="Left" Width="25" Height="25" Margin="2,0,0,0" ToolTip="Refresh UI" IsEnabled="True">
                                        <Button.Content>
                                            <Image Width="14" Source="/UIBlueprintEditor;component/Images/Refresh.png"/>
                                        </Button.Content>
                                    </Button>

                                    <Button Panel.ZIndex="10" x:Name="PART_Precise" HorizontalAlignment="Left" Width="25" Height="25" Margin="30,0,0,0" ToolTip="Precise Movement (ON)" IsEnabled="True">
                                        <Button.Content>
                                            <Image x:Name="PART_PreciseImage" Width="14" Source="/UIBlueprintEditor;component/Images/Precise_ON.png"/>
                                        </Button.Content>
                                    </Button>

                                    <Button Panel.ZIndex="10" x:Name="PART_Unhide" HorizontalAlignment="Left" Width="25" Height="25" Margin="58,0,0,0" ToolTip="Unhide all UI" IsEnabled="True">
                                        <Button.Content>
                                            <Image x:Name="PART_UnhideImage" Width="14" Source="/FrostyEditor;component/Images/Reference.png"/>
                                        </Button.Content>
                                    </Button>

                                    <TextBlock x:Name="PART_ZoomPercent" Panel.ZIndex="10" Opacity="0.75" Foreground="{StaticResource FontColor}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="100% Zoom"/>

                                    <Viewbox Grid.Row="1" Margin="40">
                                        <Grid x:Name="PART_UISize" SnapsToDevicePixels="True" VerticalAlignment="Top" HorizontalAlignment="Left">
                                            <Rectangle Fill="White" Opacity="0.02"/>

                                            <Canvas VerticalAlignment="Top" HorizontalAlignment="Left" Focusable="True" Grid.Row="1" x:Name="PART_UICanvas">
                                            </Canvas>
                                        </Grid>
                                    </Viewbox>

                                    <TextBlock Panel.ZIndex="10" x:Name="PART_UIComponentInfo" Visibility="Visible" Grid.Row="1" Margin="5,5,5,5" HorizontalAlignment="Left" VerticalAlignment="Bottom" Opacity="0.5"/>

                                    <TextBlock Panel.ZIndex="10" x:Name="PART_UISizeText" FontSize="24" Visibility="Visible" Grid.Row="1" Margin="5,5,5,5" HorizontalAlignment="Left" VerticalAlignment="Top" Opacity="0.8"/>
                                </Grid>
                            </ctrl:FrostyDockablePanel>

                            <Grid x:Name="PART_DefaultEditorLayer" Visibility="Visible">
                                <ctrl:FrostyDockablePanel Title="Properties" Icon="/FrostyEditor;component/Images/Properties.png">
                                    <core:FrostyPropertyGrid x:Name="PART_AssetPropertyGrid" Asset="{Binding Asset, RelativeSource={RelativeSource TemplatedParent}}" Modified="{Binding AssetModified, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                                </ctrl:FrostyDockablePanel>
                            </Grid>

                            <Button x:Name="PART_SwitchView" Width="24" Height="24" Margin="0,2,0,10" HorizontalAlignment="Right" VerticalAlignment="Top" ToolTip="Switch View" IsEnabled="True" Panel.ZIndex="999">
                                <Button.Content>
                                    <Image Width="14" Source="/FrostyEditor;component/Images/EditLabel.png"/>
                                </Button.Content>
                            </Button>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
